/**
 * Interface representing a policy used in the application.
 * @interface Policy
 * @property {string} title - The title of the policy.
 * @property {string} text - The text content describing the policy.
 */
export interface Policy {
    title: string, 
    text: string,
}